tflm_label_image:
  boards:
    frdmmcxn947@cm33_core0:
    - -armgcc@debug
    - -armgcc@release
    - -iar@debug
    - -iar@release
    - -mdk@debug
    - -mdk@release
    - +armgcc@flash_debug
    - +armgcc@flash_release
    - +iar@flash_debug
    - +iar@flash_release
    - +mdk@flash_debug
    - +mdk@flash_release
  contents:
    document:
      brief: Label image example for TensorFlow Lite Micro
      category: eiq_examples
      example_readme:
      - examples/eiq_examples/tflm_label_image/readme.md
      - ${board_root}/${board}/eiq_examples/tflm_label_image/example_board_readme.md
      - ${board_root}/${board}/examples_shared_readme.md
      name: tflm_label_image${core_id_suffix_name}
    meta_path: examples/eiq_examples/tflm_label_image
    project-root-path: boards/${board}/eiq_examples/tflm_label_image/${core_id}
  section-type: freestanding_application
